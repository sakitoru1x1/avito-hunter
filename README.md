# Avito Hunter 🎯

Программа для автоматического мониторинга объявлений на Avito. Задаёшь запрос и город - программа сама обновляет список каждые N минут и шлёт новые объявления в Telegram. Удобно, когда ищешь что-то конкретное (например, б/у iPhone в своём городе) и не хочешь каждый час обновлять страницу вручную.

*Инструкция рассчитана на людей без опыта с кодом. Если что-то непонятно - читай внимательно, не пропускай шаги.*

---

## Что она умеет

- Мониторит Avito по твоему запросу, городу, диапазону цен.
- Работает в фоне и сама проверяет новые объявления каждые N минут.
- Случайные задержки между запросами - чтобы Avito не заблокировал.
- Умеет работать только в заданные часы и дни недели (расписание).
- Присылает новые объявления в Telegram.
- Сохраняет историю в базу - даже если перезапустить, ничего не потеряется.
- Избранное, фильтр по рейтингу продавца, игнор-слова.
- Несколько поисковых профилей - можно быстро переключаться.

---

## Что нужно установить перед запуском

Программа написана на Python, поэтому сначала поставь три вещи. Порядок важен.

### 1. Python 3.9 или новее

- Зайди на https://www.python.org/downloads/ и скачай последнюю версию для Windows.
- При установке *обязательно поставь галочку «Add Python to PATH»* на первом экране установщика. Без неё ничего работать не будет.
- После установки открой меню Пуск → «Командная строка» (или PowerShell) и напиши:
  ```
  python --version
  ```
  Должно показать что-то вроде `Python 3.12.x`. Если пишет «не является внутренней командой» - Python не встал в PATH, переустанови с галочкой.

### 2. Google Chrome

- Нужен обычный браузер Chrome: https://www.google.com/chrome/
- Если уже стоит - ничего не делай. Программа использует твой Chrome чтобы ходить по Avito.

### 3. Git

- Скачай отсюда: https://git-scm.com/download/win
- Устанавливай со всеми настройками по умолчанию - просто жми «Next» до конца.
- Проверить можно в командной строке: `git --version`.

---

## Установка программы

1. Открой любую папку, где хочешь хранить программу (например, `C:\`).
2. В этой папке щёлкни правой кнопкой в пустое место → «Открыть в терминале» (или «Open Git Bash here»).
3. Введи команду:
   ```
   git clone https://github.com/sakitoru1x1/avito-hunter
   ```
   Появится папка `avito-hunter`.
4. Зайди в неё:
   ```
   cd avito-hunter
   ```
5. Поставь все нужные библиотеки (это делается один раз):
   ```
   pip install -r requirements.txt
   ```
   Если пишет «pip: command not found» - запусти `python -m pip install -r requirements.txt`.

---

## Запуск

Просто запусти файл `start.bat` двойным кликом. Он автоматически подтянет последнюю версию с GitHub и откроет окно программы.

Если `start.bat` не работает - открой терминал в папке программы и выполни:
```
python main.py
```

---

## Настройка Telegram-бота

Чтобы получать уведомления в Телегу, нужно создать своего бота и узнать свой Chat ID. Это делается один раз, займёт 3 минуты.

### Создаём бота через @BotFather

1. В Телеграме найди контакт *@BotFather* (официальный бот, с синей галочкой).
2. Напиши ему `/newbot`.
3. Он попросит придумать название бота (любое, например «Мой Avito-шпион»).
4. Потом попросит username - должно заканчиваться на `bot`. Например, `my_avito_spy_bot`.
5. BotFather пришлёт токен - длинная строка вроде `1234567890:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`. *Скопируй её и держи в секрете*, с ней управляют ботом.

### Узнаём Chat ID

1. Найди в Телеге контакт *@userinfobot*.
2. Напиши ему что угодно (например, «привет»).
3. Он пришлёт твой ID - число вроде `123456789`. Это и есть Chat ID.

### Вставляем в программу

1. В программе зайди на вкладку *«Настройки»* → блок «Telegram уведомления».
2. В поле «Токен бота» вставь токен из BotFather.
3. В поле «Chat ID» - число от @userinfobot.
4. Жми кнопку *«Тест»* - должно прийти тестовое сообщение в Телеге от твоего бота.

Если тестовое сообщение не пришло и ты в России - читай следующий раздел про прокси.

---

## Прокси (нужен только в России)

Роскомнадзор блокирует `api.telegram.org`, поэтому для Telegram-уведомлений нужен *зарубежный прокси*. А Avito, наоборот, лучше парсить с *российского IP* - иначе он может банить или показывать меньше объявлений.

Поэтому в программе *два отдельных прокси*:
- *Основной прокси* (блок «Прокси») - для Avito. Лучше российский.
- *Прокси для Telegram* (блок внутри «Telegram уведомления») - для бота. Только зарубежный (например, Германия, Нидерланды).

### Где взять прокси

- Платные сервисы: mobileproxy.space, proxy6.net, youproxy.ru и другие. От 200-500₽/месяц.
- Тебе нужно: *тип* (HTTP или SOCKS5), *хост*, *порт*, *логин*, *пароль*. Всё это сервис даст после оплаты.

### Как вставить

- На вкладке «Настройки» заполни поля основного прокси (Avito).
- Ниже, в блоке «Прокси для Telegram», заполни поля зарубежного прокси.
- Жми «Тест прокси» и «Тест» (рядом с Telegram) - оба должны написать «✅ ...работает».
- Если не работают - проверь логин/пароль, хост, порт, тип (HTTP/SOCKS5).

Если ты *не в России* - прокси вообще не нужны, оба блока оставь пустыми.

---

## Настройка поиска

1. Вкладка *«Результаты поиска»*.
2. Выбери *город* или поставь галку «Вся Россия».
3. В поле *«Запрос»* напиши что ищешь - например: `oculus quest 3`.
4. В поле *«Игнор»* (опционально) - слова через запятую, которые должны отсутствовать в объявлении. Пример: запрос `oculus quest 3`, игнор `3s` - чтобы отфильтровать Quest 3S (регистр не важен).
5. Цена от-до, минимальный рейтинг продавца - по вкусу.
6. Чекбоксы: «Звук» - пищит при новых, «Убрать услуги» - убирает сервисные объявления, «Авито доставка» - только объявления с доставкой.
7. Жми *«▶ Начать»* - программа разок парсит и показывает список.
8. Жми *«🔄 Авто»* - включается автоматическое обновление каждые N минут. Интервал настраивается в полях «от/до».

Все новые объявления улетают в Телегу с фото и ссылкой.

---

## Поисковые профили

Если отслеживаешь несколько разных вещей одновременно - сохрани их как профили:

1. Настрой поиск как обычно (запрос, цена, город, игнор).
2. Жми *«💾 Сохранить как профиль»* - дай имя.
3. Потом на вкладке *«Профили поиска»* можно создавать, редактировать, удалять и переключаться между ними.
4. Кнопка «✔ Сделать активным» - при следующем запуске программы будет загружен этот профиль.

---

## Расписание работы

Если не хочется, чтобы программа парсила ночью или в выходные - включи расписание:

1. Вкладка «Настройки» → блок «Расписание работы».
2. Поставь галку «Работать только по расписанию».
3. Выбери дни недели и промежуток времени (например, Пн-Пт 09:00-21:00).
4. Сохрани. Теперь авто-обновление работает только в эти часы.

---

## FAQ

*Программа не запускается, пишет «No module named...»*
Ты забыл выполнить `pip install -r requirements.txt`. Открой терминал в папке программы и выполни.

*Chrome открывается и сразу закрывается / пишет про драйвер*
Обнови Chrome до последней версии. Selenium сам подтянет подходящий драйвер при первом запуске - нужно интернет-соединение.

*В Телегу ничего не приходит, а Avito парсится нормально*
Ты в России и у тебя не настроен прокси для Telegram. Читай раздел про прокси.

*Объявления вообще не появляются*
Проверь: правильный ли город, не слишком ли узкий диапазон цен, не слишком ли строгий рейтинг продавца. Попробуй временно сбросить все фильтры.

*Avito начал показывать капчу или пустой список*
Ты слишком часто парсишь. Увеличь интервал (например, «от 5 до 10 минут»), включи расписание, используй прокси. Авито не любит роботов.

*Карточки прыгают местами после авто-обновления*
Они отсортированы по времени публикации. Если у нескольких объявлений одинаковая метка времени («сегодня») - они располагаются по времени добавления в программу.

*Можно ли использовать без Телеги?*
Да. Оставь поля токена и Chat ID пустыми - программа будет просто показывать новые объявления в интерфейсе.

---

## Файлы и данные

- `avito_data.db` - твоя локальная база со всей историей. Не удаляй, если не хочешь потерять избранное и историю цен.
- `settings.json` - настройки (токены, прокси, расписание). Хранится только у тебя.
- `parser.log` - логи работы программы. Туда пишется всё, что происходит.

Эти файлы в `.gitignore` и не выгружаются в репозиторий.

---

## Для специалистов — технические подробности

<details>
<summary>Развернуть технический раздел: архитектура, оптимизации, внутреннее устройство</summary>

### Общая архитектура

Программа состоит из нескольких слабосвязанных модулей, объединённых через `gui.py` как точку оркестрации. Парсинг выполняется в фоновом потоке, UI — в главном; коммуникация идёт через `root.after(...)` и разделяемое состояние `self.all_items`.

| Модуль | Ответственность |
|---|---|
| `main.py` | Точка входа, инициализация CustomTkinter |
| `gui.py` | UI (CustomTkinter), оркестрация парсинга, рендер карточек, авто-обновление, расписание |
| `driver.py` | Менеджер Selenium-драйвера Chrome: запуск, переподключение к упавшей сессии, прокси, undetected_chromedriver |
| `database.py` | SQLite: схема, `get_conn()` с `PRAGMA journal_mode=WAL` и `synchronous=NORMAL`, батч-транзакции |
| `storage.py` | Мердж списка новых результатов со старыми (`update_all_items`), выставление `is_new`, экспорт/импорт JSON |
| `telegram.py` | HTTP-клиент к Bot API: изолированная `requests.Session` с `trust_env=False`, ретраи на флаковом VPN |
| `errors.py` | `format_user_error(exc, context)` — превращает raw-исключения в читаемые для юзера сообщения; `should_retry`, `backoff_seconds` |
| `utils.py` | Парсинг дат (`сегодня`, `вчера`, `2 часа назад` → timestamp), санитайзеры для HTML в Telegram |
| `config.py` | Константы: `SETTINGS_FILE`, `DB_FILE`, USER_AGENTS и т.п. |
| `logger_setup.py` | Ротация логов, глобальный обработчик с отправкой краш-репорта в Telegram |

### Telegram-уведомления: устойчивость к VPN

Ключевая проблема — при включённом VPN-клиенте (OpenVPN/WireGuard) системные переменные `HTTPS_PROXY`/`HTTP_PROXY`/`NO_PROXY` подставляются драйвером и ломают маршрут до `api.telegram.org`. Решение — изолированная сессия:

```python
def _make_session():
    s = requests.Session()
    s.trust_env = False
    return s
```

Поверх этого — обёртка с ретраями на `Timeout`/`ConnectionError` (`_post_with_retry` в `telegram.py`), backoff = 3, 6 секунд, таймаут `sendMessage` = 30s, `sendPhoto` = 45/60s. Этого хватает, чтобы пережить секундные замирания VPN-туннеля без потери уведомлений.

Прокси для Telegram отделён от основного прокси для Avito (`tg_proxy_*` в settings) — типичный кейс: РФ-прокси для Авито + зарубежный для обхода блокировки Bot API.

*Диагностическое правило*: если в `parser.log` нет ошибок отправки (return 200), но юзер не видит уведомлений — проблема на стороне TG-клиента (Desktop/Mobile не пробивается через VPN), а не в нашем коде. Прописать тот же прокси в самом TG-клиенте.

### Инкрементальный парсер (feat/incremental-parser)

Изначально `parse_items` извлекал все поля (title, price, image_url, description, seller_rating, фильтры по цене) для каждой карточки на странице (~50-100 штук) на каждом цикле — хотя новых объявлений обычно 0-5. 95+ карточек обрабатывались впустую.

Решение — двухпроходный `parse_items`, возвращающий `(full_new_items, page_summary)`:

**Pass 1 (по всем карточкам, дёшево):**
- `get_item_id` читает `data-item-id` без обращения к подэлементам
- `extract_date` читает `[data-marker='item-date']`, прогоняется через `parse_date_to_timestamp`
- Собирается `page_summary = [{"id", "pub_date_timestamp", "search_query"}, ...]`

**Pass 2 (только для новых):**
- Кеш `self._known_ids_cache = {item["id"] for item in self.all_items}` строится в `run_parser`
- Полный парсинг применяется только к карточкам, чьи ID не в кеше

`_detect_disappeared` работает как раньше — получает `page_summary` со всеми ID страницы и вычисляет `min_ts = min(...)` для границы страницы. VIP-блоки Авито инжектит непредсказуемо, поэтому dedup идёт через set ID (внутри `update_all_items` в `storage.py`), не через «остановиться на первом известном».

### Пайплайн изображений

Картинки загружаются в два этапа:

**1. Извлечение URL (batch-JS + прогрессивный скролл).** Вместо того чтобы для каждой карточки дёргать `driver.find_element(...).get_attribute("src")` (50 roundtrip'ов к Chrome), выполняется один JS-скрипт, возвращающий массив `[{id, src}, ...]` со всей страницы. Перед этим делается постепенный скролл с шагом и `sleep 0.35s` — lazy-loaded `<img>` успевают подгрузить реальный `src` (не `data-src` placeholder). Fallback: если `src` пустой — берётся `data-src`.

**2. Скачивание изображения (async, per-card).** При рендере карточки в `_create_card` создаётся `requests.Session` с cookies из Selenium и UA из `USER_AGENTS`. Загрузка идёт через `ThreadPoolExecutor` — UI не блокируется. На каждую карточку — отдельный future.

**3. Retry старых URL.** URL на Авито бывают подписанными и истекают. Если при загрузке картинки прилетает 403 — на следующем цикле парсинга этот ID попадает в `retry_updated_items`, его URL переизвлекается заново и перезаписывается в БД.

### Оптимизации БД

SQLite изначально настроен на скорость:
- `PRAGMA journal_mode=WAL` — writers не блокируют readers
- `PRAGMA synchronous=NORMAL` — fsync после транзакции, не после каждой записи
- `save_ads` оборачивает все upsert'ы в одну транзакцию (`with get_conn()`)

*Узкое место, которое устранили:* `save_data(self.all_items)` на каждом цикле парсинга писал все 500+ строк вне зависимости от того, изменилось что-то или нет. Заменено на:

```python
dirty = []
if new_results:
    dirty.extend(new_results)
if retry_updated_items:
    dirty.extend(retry_updated_items)
# дедуп по id
if dirty:
    save_data(dirty, self.log)
```

Теперь типичный цикл «0 новых» = 0 записей в БД. `last_seen` не трогаем при каждом проходе (нигде не используется как фильтр, можно обновлять лениво).

Hover-handler на карточке раньше делал `save_data(self.all_items)` при каждом `<Enter>` (писал всё). Заменено на `database.upsert_ad(_item)` — одна строка.

### Оптимизации GUI (fast-path в display_results)

CustomTkinter не умеет инкрементально обновлять список карточек — при перерисовке приходится уничтожать старые `CTkFrame` и создавать новые. Для 50 карточек × несколько `CTkWidget` в каждой это ~0.3-1 сек лага на каждый цикл авто-обновления, даже когда ничего не изменилось.

Решение — fast-path:

```python
def display_results(self):
    visible_items = [it for it in self.all_items if (not fav_only) or it.get("is_favorite")]
    visible_ids = [it["id"] for it in visible_items]

    # Fast path: тот же порядок ID → только синхронизируем is_new цвет
    if self._rendered_order == visible_ids and self._rendered_cards is not None:
        for id_, info in self._rendered_cards.items():
            new_is_new = items_by_id[id_].get("is_new", False)
            if info["state"]["is_new"] != new_is_new:
                info["state"]["is_new"] = new_is_new
                info["card"].configure(fg_color=info["get_color"]())
        return

    # Slow path: полный ребилд + запоминаем _rendered_cards/_rendered_order
```

Инвалидация fast-path (`self._rendered_order = None`) вызывается только когда реально нужно: новые объявления, retry-обновления, переключение избранного, смена профиля поиска.

Типичный кейс «авто-цикл, 0 новых» = 0 destroy/create виджетов, только обход словаря с проверкой `is_new`.

### Фоновой поток парсинга

`run_parser` крутится в `threading.Thread(daemon=True)`. Взаимодействие с Tk — только через `self.root.after(0, lambda: ...)`. Состояние `self.all_items` читается/пишется из обоих потоков, но т.к. Python GIL и операции над списком атомарны, блокировки не нужны. Если парсинг упал — исключение ловится в try/except верхнего уровня, `format_user_error` даёт юзеру понятное сообщение, полный traceback идёт в `parser.log`.

### Глобальный краш-репорт

В `logger_setup.py` установлен `sys.excepthook`, который при падении главного потока формирует traceback, санитизирует его (`sanitize_error_for_telegram`) и шлёт в Telegram через отдельную сессию (`trust_env=False`). Это работает даже если `gui.py` успел упасть — важно для дебага у юзеров, которые не умеют читать `parser.log`.

</details>

---

## Проблемы и баги

Если что-то не работает - открой Issue на GitHub: https://github.com/sakitoru1x1/avito-hunter/issues

Приложи лог из `parser.log` и опиши что именно не работает.
